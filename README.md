# PWA Карточки (TypeScript)

Простое PWA приложение для просмотра карточек с навигацией через стрелки и свайпы. Написано на TypeScript с полной типизацией.

## 🚀 Возможности

- ✅ **Написано на TypeScript** с полной типизацией
- ✅ Отображение карточек по одной на экране
- ✅ Навигация через кнопки-стрелки
- ✅ Навигация через свайпы (влево/вправо)
- ✅ Навигация через клавиши стрелок на клавиатуре
- ✅ Индикатор прогресса (X / Total)
- ✅ Сохранение позиции в localStorage
- ✅ Адаптивный дизайн для всех устройств
- ✅ Автоматическое масштабирование шрифта
- ✅ Плавная анимация смены карточек
- ✅ PWA с offline-поддержкой

## 📁 Структура проекта

```
pwa-cards/
├── src/                # Исходные TypeScript файлы
│   ├── app.ts          # Основная логика приложения
│   ├── sw.ts           # Service Worker
│   └── types.ts        # TypeScript интерфейсы и типы
├── dist/               # Скомпилированные файлы (генерируется автоматически)
│   ├── app.js          # Скомпилированный app.ts
│   ├── sw.js           # Скомпилированный sw.ts
│   └── ...             # Статические файлы (HTML, CSS, JSON)
├── index.html          # Главная страница
├── styles.css          # Стили
├── cards.json          # Данные карточек
├── manifest.json       # PWA манифест
├── tsconfig.json       # Конфигурация TypeScript
├── package.json        # Зависимости проекта
└── icons/              # Иконки PWA
    ├── icon-192.png
    └── icon-512.png
```

## 🛠️ Установка и запуск

### 1. Установка зависимостей

```bash
npm install
```

Это установит:
- `typescript` - компилятор TypeScript
- `http-server` - локальный веб-сервер для разработки

### 2. Добавление своих карточек

Отредактируйте файл `cards.json`:

```json
[
  {
    "id": 1,
    "text": "Текст первой карточки"
  },
  {
    "id": 2,
    "text": "Текст второй карточки"
  }
]
```

### 3. Сборка проекта

```bash
npm run build
```

Эта команда:
1. Компилирует TypeScript файлы из `src/` в `dist/`
2. Копирует статические файлы (HTML, CSS, JSON, иконки) в `dist/`

### 4. Запуск в режиме разработки

```bash
npm run dev
```

Откройте в браузере: `http://localhost:8000`

### 5. Режим watch (автоматическая пересборка)

Для автоматической пересборки при изменении файлов:

```bash
npm run watch
```

В отдельном терминале запустите сервер:

```bash
npx http-server dist -p 8000
```

### 6. Установка как PWA

1. Откройте приложение в Chrome/Edge/Safari
2. В меню браузера выберите "Установить приложение" или нажмите иконку установки в адресной строке
3. Приложение появится на рабочем столе/домашнем экране

## 📝 Формат данных

Данные карточек находятся в файле `cards.json`:

```json
[
  {
    "id": 1,
    "text": "Текст карточки"
  }
]
```

### TypeScript типы

Интерфейсы определены в `src/types.ts`:

```typescript
export interface Card {
  id: number;
  text: string;
}

export interface AppState {
  cards: Card[];
  currentIndex: number;
  isAnimating: boolean;
}

export interface TouchState {
  startX: number;
  endX: number;
}
```

## 🎮 Управление

- **Кнопки ←/→** - навигация между карточками
- **Свайп влево/вправо** - навигация на touch-устройствах
- **Клавиши ←/→** - навигация с клавиатуры
- **Счетчик внизу** - показывает текущую позицию

## 🔧 Настройка TypeScript

### tsconfig.json

Основные настройки компилятора:

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "ES2020",
    "strict": true,
    "outDir": "./dist",
    "rootDir": "./src"
  }
}
```

### Изменение порога свайпа

В `src/app.ts` измените константу:

```typescript
const SWIPE_THRESHOLD = 50; // пикселей
```

### Изменение длительности анимации

В `src/app.ts` измените константу:

```typescript
const ANIMATION_DURATION = 300; // миллисекунд
```

## 🌐 Offline-работа

Приложение использует Service Worker для кэширования всех файлов. После первой загрузки приложение работает полностью offline.

При обновлении файлов очистите кэш:
1. DevTools → Application → Storage → Clear site data
2. Или измените версию кэша в `src/sw.ts`: `CACHE_NAME = 'pwa-cards-v3'`

## 📱 Совместимость

- ✅ Chrome/Edge 90+
- ✅ Safari 14+
- ✅ Firefox 90+
- ✅ iOS Safari 14+
- ✅ Chrome Android

## 🐛 Устранение проблем

**Service Worker не регистрируется:**
- HTTPS или localhost обязателен для PWA
- Проверьте консоль браузера на ошибки

**Ошибки компиляции TypeScript:**
- Убедитесь, что установлены зависимости: `npm install`
- Проверьте версию TypeScript: `npx tsc --version`

**Изменения не применяются:**
- Пересоберите проект: `npm run build`
- Очистите кэш браузера

## 🧪 Разработка

### Структура исходного кода

- `src/app.ts` - основная логика приложения с типизацией
- `src/sw.ts` - Service Worker с типами для WebWorker API
- `src/types.ts` - общие интерфейсы и типы

### Команды NPM

- `npm run build` - сборка проекта
- `npm run watch` - режим наблюдения за изменениями
- `npm run dev` - сборка и запуск dev-сервера
- `npm run clean` - очистка папки dist

## 📄 Лицензия

MIT License - используйте свободно для любых целей.

!!!!!!