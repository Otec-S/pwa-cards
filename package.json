{
  "name": "pwa-cards",
  "version": "1.0.0",
  "description": "PWA приложение для просмотра карточек на TypeScript",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "dev": "npm run build && npx http-server . -p 8000",
    "clean": "rm -f app.js sw.js types.js"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "http-server": "^14.1.1"
  }
}
